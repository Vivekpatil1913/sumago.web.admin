# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
*.zip
sumga.zip
                
# testing
/coverage

# next.js
/.next/
/out/

node_modules/
dist/
build/
*.zip
.env

# production
/build

# misc
.DS_Store
*.pem

# Master photography. The originals (hundreds of MB of raw camera/WhatsApp
# files) live here and are never served: `npm run build:images` derives the
# optimised webp the site actually ships into public/images/.
/assets-source/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Output of scripts/responsive-audit.mjs --shots (local review artefacts).
screenshots/
